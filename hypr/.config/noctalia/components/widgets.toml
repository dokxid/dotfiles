[bar.main]
icon_color = "on_surface"
start = ["workspaces"]
center = ["weekday", "gap", "clock", "gap", "date"]
end = ["group:g1", "gap", "tray", "gap", "notifications"]

[[bar.main.capsule_group]]
id = "g1"
members = ["network", "bluetooth", "volume"]
fill = "#00000000"
foreground = "on_surface"
padding = 6.0
opacity = 0.9
enabled = true

[widget.notifications]
capsule = true
capsule_padding = 16
capsule_fill = "primary"
icon_color = "on_primary"
capsule_foreground = "on_primary"

[widget.gap]
type = "spacer"
length = 12

[widget.workspaces]
display = "name"                  # none | id | name
minimal = false                   # true = text-only labels, no animated pills
max_label_chars = 8               # truncate long workspace names (e.g. "VESKTOP" → "VE")
labels_only_when_occupied = false # (1) (2*) ( ) (4) - occupied tags and the active tag show labels (*active but empty)
hide_when_empty = false
pill_scale = 1.5
active_pill_size = 2.2
inactive_pill_size = 1.0
focused_color = "primary"
occupied_color = "secondary"
empty_color = "surface_variant"
focused_output_only = true        # true = only the focused monitor highlights its active workspace

[widget.volume]
show_label = false

[widget.network]
show_label = false

[widget.tray]
capsule = true
icon_color = "on_surface"
capsule_fill = "#00000000"
hidden = [
  "nm-applet",
  "blueman",
  "org.kde.StatusNotifierItem-1-1",
  "tailscale",
  "keepassxc",
]

[widget.clock]
scale = 1
format = "{:%H:%M}"
capsule_fill = "primary"
capsule_foreground = "on_primary"
capsule = true
capsule_padding = 16

[widget.weekday]
type = "clock"
scale = 0.8
format = "KW {:%W - %a}"

[widget.date]
type = "clock"
scale = 0.8
format = "{:%d.%m.%Y}"
